# ── PostgreSQL / pgvector ────────────────────────────
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=databse

# Connection string used by the API gateway and any Python service.
DATABASE_URL=postgresql://username:password@db:5432/databse
